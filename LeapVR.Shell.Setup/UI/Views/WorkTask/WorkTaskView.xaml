<UserControl
    x:Class="LeapVR.Shell.Setup.UI.Views.WorkTask.WorkTaskView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:lex="http://wpflocalizeextension.codeplex.com" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300" d:DesignWidth="300"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="500" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <lex:LocProxy x:Name="Proxy" Source="{Binding Path=Key, Mode=OneWay}" />
        <TextBlock
            Grid.Column="0"
            Margin="15" VerticalAlignment="Center"
            FontSize="20"
            Text="{Binding Result, ElementName=Proxy}" />
        <Button
            x:Name="DoWork"
            Grid.Column="1"
            Width="45" Height="45"
            materialDesign:ButtonProgressAssist.IsIndeterminate="{Binding IsProgress}"
            materialDesign:ButtonProgressAssist.IsIndicatorVisible="{Binding IsProgress}"
            materialDesign:ButtonProgressAssist.Value="-1"
            Style="{StaticResource MaterialDesignFloatingActionButton}">
            <!--  simple example of toggling/animating pack icon with a data trigger  -->
            <materialDesign:PackIcon Width="30" Height="30">
                <materialDesign:PackIcon.Style>
                    <Style BasedOn="{StaticResource {x:Type materialDesign:PackIcon}}" TargetType="materialDesign:PackIcon">
                        <Setter Property="Kind" Value="Loading" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsDone}" Value="True">
                                <Setter Property="Kind" Value="Check" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsError}" Value="True">
                                <Setter Property="Kind" Value="CloseCircle" />
                                <Setter Property="Foreground" Value="Red" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </materialDesign:PackIcon.Style>
            </materialDesign:PackIcon>
        </Button>
    </Grid>
</UserControl>