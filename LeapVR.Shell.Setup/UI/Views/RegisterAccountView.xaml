<UserControl
    x:Class="LeapVR.Shell.Setup.UI.Views.RegisterAccountView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lex="http://wpflocalizeextension.codeplex.com"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="500"
    d:DesignWidth="600"
    lex:ResxLocalizationProvider.DefaultAssembly="LeapVR.Shell.Language"
    lex:ResxLocalizationProvider.DefaultDictionary="Resources"
    mc:Ignorable="d">
    <UniformGrid MaxWidth="600" Columns="1">
        <!--GroupBox
            Grid.Row="0"
            Height="auto"
            Margin="5"
            Header="{lex:Loc Config_Activate_License}"
            Style="{DynamicResource MaterialDesignCardGroupBox}">
            <GroupBox.HeaderTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon
                            Width="40"
                            Height="40"
                            VerticalAlignment="Center"
                            Kind="Account" />
                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Style="{StaticResource MaterialDesignTitleTextBlock}"
                            Text="{Binding}" />
                    </StackPanel>
                </DataTemplate>
            </GroupBox.HeaderTemplate>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Button
                    Grid.Row="0"
                    Grid.Column="0"
                    Width="55"
                    Height="55"
                    materialDesign:ShadowAssist.ShadowDepth="Depth4"
                    IsHitTestVisible="False"
                    Style="{StaticResource MaterialDesignFloatingActionDarkButton}">
                    <TextBlock
                        FontSize="30"
                        FontWeight="Bold"
                        TextAlignment="Center">
                        1
                    </TextBlock>
                </Button>
                <StackPanel
                    Grid.Row="0"
                    Grid.Column="1"
                    Margin="8,0,0,0"
                    VerticalAlignment="Center">
                    <Border
                        x:Name="Border"
                        Margin="10,0,10,0"
                        Padding="0,5,0,5"
                        BorderBrush="{DynamicResource MaterialDesignDivider}"
                        BorderThickness="0,0,0,1">
                        <TextBlock
                            VerticalAlignment="Center"
                            FontWeight="DemiBold"
                            Text="{lex:Loc Config_Register_VisitActivationSite}"
                            TextAlignment="Left" />
                    </Border>
                    <Button
                        x:Name="OpenWebsiteNewAccount"
                        Margin="10"
                        Background="{DynamicResource PrimaryHueMidBrush}"
                        Content="{lex:Loc Config_Register_NewAccountBtn}"
                        Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"
                        ToolTip="https://activate.example.com/ActivationEntryForNewUser" />
                    <Button
                        x:Name="OpenWebsiteExistingAccount"
                        Margin="10"
                        Background="{DynamicResource PrimaryHueMidBrush}"
                        Content="{lex:Loc Config_Register_ExistingAccountBtn}"
                        Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"
                        ToolTip="https://activate.example.com/ActivationEntryForExistingUser" />
                </StackPanel>
                <Button
                    Grid.Row="1"
                    Grid.Column="0"
                    Width="55"
                    Height="55"
                    materialDesign:ShadowAssist.ShadowDepth="Depth4"
                    IsHitTestVisible="False"
                    Style="{StaticResource MaterialDesignFloatingActionDarkButton}">
                    <TextBlock
                        FontSize="30"
                        FontWeight="Bold"
                        TextAlignment="Center">
                        2
                    </TextBlock>
                </Button>
                <StackPanel
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="8,0,0,0"
                    VerticalAlignment="Center">
                    <Border
                        Margin="10,0,10,0"
                        Padding="0,5,0,5"
                        BorderBrush="{DynamicResource MaterialDesignDivider}"
                        BorderThickness="0,0,0,1">
                        <StackPanel>
                            <TextBlock
                                VerticalAlignment="Center"
                                FontWeight="DemiBold"
                                Text="{lex:Loc Config_Register_AddLicenseToAccount}"
                                TextAlignment="Left" />
                        </StackPanel>
                    </Border>
                    <TextBlock
                        Margin="10"
                        Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"
                        Text="{lex:Loc Config_Register_AddLicenseToAccount_Text}"
                        TextAlignment="Left"
                        TextWrapping="Wrap" />
                </StackPanel>
                <Button
                    Grid.Row="2"
                    Grid.Column="0"
                    Width="55"
                    Height="55"
                    materialDesign:ShadowAssist.ShadowDepth="Depth4"
                    IsHitTestVisible="False"
                    Style="{StaticResource MaterialDesignFloatingActionDarkButton}">
                    <TextBlock
                        FontSize="30"
                        FontWeight="Bold"
                        TextAlignment="Center">
                        3
                    </TextBlock>
                </Button>
                <StackPanel
                    Grid.Row="2"
                    Grid.Column="1"
                    Margin="8,0,0,0"
                    VerticalAlignment="Center">
                    <Border
                        Margin="10,0,10,0"
                        Padding="0,5,0,5"
                        BorderBrush="{DynamicResource MaterialDesignDivider}"
                        BorderThickness="0,0,0,1">
                        <StackPanel>
                            <TextBlock
                                VerticalAlignment="Center"
                                FontWeight="DemiBold"
                                Text="{lex:Loc Config_Register_Done}"
                                TextAlignment="Left" />
                        </StackPanel>
                    </Border>
                    <StackPanel>
                        <TextBlock
                            Margin="10,10,0,0"
                            Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"
                            Text="{lex:Loc Config_Register_Done_Text}"
                            TextAlignment="Left"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </-->
        <TextBlock
            MaxHeight="500"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="30"
            FontWeight="Bold"
            Text="{lex:Loc Config_Register_Done}" />
    </UniformGrid>
</UserControl>