<UserControl
    x:Class="LeapVR.Shell.UI.Universal.Platform.Views.PlatformSelectorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cal="http://www.caliburnproject.org" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:LeapVR.Shell.UI.Shell.SystemAdministration.Applications.Platform.Views" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="100" d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Height="auto">

        <Rectangle
            Margin="20"
            Fill="Black" Grid.ZIndex="10" Opacity="0.5" RadiusX="10"
            RadiusY="10"
            Visibility="{Binding EnableSelect, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=true, FallbackValue=Collapsed}" />
        <ListBox
            x:Name="Platforms"
            Background="Transparent" BorderThickness="0"
            IsEnabled="{Binding EnableSelect}"
            ItemContainerStyle="{DynamicResource SelectionStyle}"
            SelectionMode="Single">

            <ListBox.Resources>
                <Style xmlns:s="clr-namespace:System;assembly=mscorlib" TargetType="ListBox">
                    <Style.Resources>
                        <ResourceDictionary />
                    </Style.Resources>
                    <Setter Property="Panel.Background">
                        <Setter.Value>
                            <DynamicResource ResourceKey="{x:Static SystemColors.WindowBrushKey}" />
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Border.BorderBrush">
                        <Setter.Value>
                            <SolidColorBrush>Transparent</SolidColorBrush>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Border.BorderThickness">
                        <Setter.Value>
                            <Thickness>0</Thickness>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="TextElement.Foreground">
                        <Setter.Value>
                            <DynamicResource ResourceKey="{x:Static SystemColors.ControlTextBrushKey}" />
                        </Setter.Value>
                    </Setter>
                    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility">
                        <Setter.Value>
                            <x:Static Member="ScrollBarVisibility.Hidden" />
                        </Setter.Value>
                    </Setter>
                    <Setter Property="ScrollViewer.VerticalScrollBarVisibility">
                        <Setter.Value>
                            <x:Static Member="ScrollBarVisibility.Hidden" />
                        </Setter.Value>
                    </Setter>
                    <Setter Property="ScrollViewer.CanContentScroll">
                        <Setter.Value>
                            <s:Boolean>False</s:Boolean>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Control.VerticalContentAlignment">
                        <Setter.Value>
                            <x:Static Member="VerticalAlignment.Center" />
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Control.Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBox">
                                <Border
                                    Name="Bd"
                                    Padding="0"
                                    Background="{TemplateBinding Panel.Background}"
                                    BorderBrush="{TemplateBinding Border.BorderBrush}"
                                    BorderThickness="{TemplateBinding Border.BorderThickness}"
                                    SnapsToDevicePixels="True">
                                    <ScrollViewer Padding="{TemplateBinding Control.Padding}" Focusable="False">
                                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
                                    </ScrollViewer>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="UIElement.IsEnabled">
                                        <Setter TargetName="Bd" Property="Panel.Background" Value="Transparent" />
                                        <Trigger.Value>
                                            <s:Boolean>False</s:Boolean>
                                        </Trigger.Value>
                                    </Trigger>
                                    <Trigger Property="ItemsControl.IsGrouping">
                                        <Setter Property="ScrollViewer.CanContentScroll">
                                            <Setter.Value>
                                                <s:Boolean>False</s:Boolean>
                                            </Setter.Value>
                                        </Setter>
                                        <Trigger.Value>
                                            <s:Boolean>True</s:Boolean>
                                        </Trigger.Value>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
                <Style x:Key="SelectionStyle" TargetType="{x:Type ListBoxItem}">
                    <Setter Property="Foreground" Value="Transparent" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderBrush" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Margin" Value="5" />

                    <Setter Property="TextBlock.TextTrimming" Value="CharacterEllipsis" />
                    <Setter Property="UseLayoutRounding" Value="False" />
                    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                    <!--  Setter Property="Template" Value="{DynamicResource PlatformContainerTemplate}" /  -->
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Template" Value="{DynamicResource UnselectedPlatformContainerTemplate}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="False">
                            <Setter Property="Template" Value="{DynamicResource PlatformContainerTemplate}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
                <ControlTemplate x:Key="PlatformContainerTemplate" TargetType="{x:Type ListBoxItem}">
                    <Border
                        x:Name="Border"
                        Width="auto" Height="auto"
                        HorizontalAlignment="Center" VerticalAlignment="Center"
                        Background="Transparent" BorderBrush="Transparent" BorderThickness="3" CornerRadius="5">
                        <ContentPresenter x:Name="ContentSite" />
                    </Border>
                </ControlTemplate>
                <ControlTemplate x:Key="UnselectedPlatformContainerTemplate" TargetType="{x:Type ListBoxItem}">
                    <Border
                        x:Name="Border"
                        Width="auto" Height="auto"
                        HorizontalAlignment="Center" VerticalAlignment="Center"
                        Background="Transparent" BorderBrush="White" BorderThickness="3" CornerRadius="5">
                        <ContentPresenter x:Name="ContentSite" />
                    </Border>
                </ControlTemplate>
            </ListBox.Resources>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid
                        HorizontalAlignment="Left"
                        Background="Transparent" IsItemsHost="True" KeyboardNavigation.TabIndex="1" Rows="1" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ListBox>
    </Grid>
</UserControl>