<UserControl
    x:Class="LeapVR.Shell.UI.Universal.Platform.Views.PlatformAccountsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:LeapVR.Shell.UI.Core.Behaviors" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity" xmlns:lex="http://wpflocalizeextension.codeplex.com"
    xmlns:local="clr-namespace:LeapVR.Shell.UI.Universal.Platform.Views" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:usercontrols="clr-namespace:LeapVR.Shell.UI.Usercontrols"
    d:DesignHeight="100" d:DesignWidth="1300"
    mc:Ignorable="d">

    <UserControl.Resources>
        <!--<Style BasedOn="{StaticResource TextBlockBaseStyle}" TargetType="TextBlock">
            <Setter Property="FontSize" Value="{DynamicResource H5}" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>-->
    </UserControl.Resources>
    <UniformGrid Height="70" Columns="2">
        <StackPanel Grid.Column="0" Orientation="Horizontal">
            <TextBlock
                HorizontalAlignment="Center" VerticalAlignment="Center"
                FontSize="{DynamicResource LoginViewsH5}"
                Foreground="{DynamicResource ThemeMainTextColor}"
                TextAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="{x:Type TextBlock}">
                        <Setter Property="Text" Value="{lex:BLoc System_Platform_NoPlatformAvailible}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding PlatformSelector.HasPlatforms}" Value="True">
                                <Setter Property="Text" Value="{lex:BLoc System_Platform_SelectPlatform}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <ContentControl x:Name="PlatformSelector" />
        </StackPanel>
        <Grid Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <TextBlock
                    MinWidth="100"
                    Margin="0,0,20,0"
                    FontSize="{DynamicResource H5}"
                    Style="{DynamicResource TextBlockBaseStyle}"
                    Text="{lex:Loc System_Platform_Account}" />
                <usercontrols:LeapDropDown
                    x:Name="PlatformAccounts"
                    MinWidth="300"
                    BorderBrush="{DynamicResource ThemeSecondaryTextColor}"
                    BorderThickness="3" IsEditable="False" PopupPlacement="Bottom">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    FontSize="{DynamicResource H5}"
                                    Style="{DynamicResource TextBlockBaseStyle}"
                                    Text="{Binding DisplayName}"
                                    TextAlignment="Center" />
                            </Grid>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </usercontrols:LeapDropDown>
            </StackPanel>
            <UniformGrid Grid.Column="1" Margin="50,0,50,0" Columns="2">
                <usercontrols:LeapButton
                    x:Name="AddAccount"
                    Margin="5" HorizontalAlignment="Stretch"
                    BorderThickness="1" Focusable="True"
                    IsEnabled="{Binding CanAddAcount}"
                    Style="{DynamicResource ButtonBaseStyle}">
                    <usercontrols:LeapButton.Content>
                        <TextBlock Margin="10" VerticalAlignment="Center" FontSize="{DynamicResource H5}" Text="{lex:Loc System_Platform_Account_Create}" />
                    </usercontrols:LeapButton.Content>
                </usercontrols:LeapButton>
                <usercontrols:LeapButton
                    x:Name="DeleteAccount"
                    Margin="5" HorizontalAlignment="Stretch"
                    BorderThickness="1" Focusable="True"
                    FontSize="{DynamicResource H5}"
                    IsEnabled="{Binding CanDeleteAccount}"
                    Style="{DynamicResource ButtonBaseStyle}">
                    <usercontrols:LeapButton.Content>
                        <TextBlock Margin="10" VerticalAlignment="Center" FontSize="{DynamicResource H5}" Text="{lex:Loc System_Platform_Account_Delete}" />
                    </usercontrols:LeapButton.Content>
                </usercontrols:LeapButton>
            </UniformGrid>
        </Grid>
    </UniformGrid>
</UserControl>