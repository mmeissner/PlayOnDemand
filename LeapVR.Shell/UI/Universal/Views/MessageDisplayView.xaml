<UserControl
    x:Class="LeapVR.Shell.UI.Universal.Views.MessageDisplayView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="720" d:DesignWidth="1280"
    mc:Ignorable="d">
    <Grid>
        <StackPanel VerticalAlignment="Center">
            <TextBlock
                VerticalAlignment="Center"
                FontSize="{DynamicResource H3}"
                Foreground="{DynamicResource ThemeMainTextColor}"
                Text="{Binding Title, Mode=OneWay, FallbackValue=title, TargetNullValue=title}"
                TextAlignment="Center" />
            <TextBlock
                VerticalAlignment="Center"
                FontSize="{DynamicResource H5}"
                Foreground="{DynamicResource ThemeMainTextColor}"
                Text="{Binding Description, Mode=OneWay, FallbackValue=description goes here}"
                TextAlignment="Center" />

        </StackPanel>
    </Grid>
</UserControl>
