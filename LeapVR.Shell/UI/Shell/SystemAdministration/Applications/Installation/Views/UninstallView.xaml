<UserControl
    x:Class="LeapVR.Shell.UI.Shell.SystemAdministration.Applications.Installation.Views.UninstallView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions" xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity" xmlns:lex="http://wpflocalizeextension.codeplex.com"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:properties="clr-namespace:LeapVR.Shell.Properties"
    d:DesignHeight="1080" d:DesignWidth="720"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style BasedOn="{StaticResource TextBlockBaseStyle}" TargetType="TextBlock">
            <Setter Property="FontSize" Value="{DynamicResource H3}" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <i:Interaction.Triggers>
            <ei:DataTrigger Binding="{Binding ItemsLoadingState}" Value="Nothing">
                <ei:ChangePropertyAction PropertyName="Visibility" TargetObject="{Binding ElementName=NoApplicationsView}" TargetName="NoApplicationsView" Value="Visible" />
                <ei:ChangePropertyAction PropertyName="Visibility" TargetObject="{Binding ElementName=LoadingView}" TargetName="LoadingView" Value="Collapsed" />
            </ei:DataTrigger>
            <ei:DataTrigger Binding="{Binding ItemsLoadingState}" Value="Loading">
                <ei:ChangePropertyAction PropertyName="Visibility" TargetObject="{Binding ElementName=NoApplicationsView}" TargetName="NoApplicationsView" Value="Collapsed" />
                <ei:ChangePropertyAction PropertyName="Visibility" TargetObject="{Binding ElementName=LoadingView}" TargetName="LoadingView" Value="Visible" />
            </ei:DataTrigger>
            <ei:DataTrigger Binding="{Binding ItemsLoadingState}" Value="Loaded">
                <ei:ChangePropertyAction PropertyName="Visibility" TargetObject="{Binding ElementName=NoApplicationsView}" TargetName="NoApplicationsView" Value="Collapsed" />
                <ei:ChangePropertyAction PropertyName="Visibility" TargetObject="{Binding ElementName=LoadingView}" TargetName="LoadingView" Value="Collapsed" />
            </ei:DataTrigger>
        </i:Interaction.Triggers>



        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="100" />
        </Grid.RowDefinitions>
        <!--  Top Bar  -->
        <Grid>
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Orientation="Horizontal" Visibility="{Binding IsFreeSpaceReady, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=true}">
                <Border Margin="5,15,5,15">
                    <UserControl Template="{DynamicResource AppleSpinner}" Visibility="{Binding IsFreeSpaceReady, Converter={StaticResource BoolToVisibilityConverter}}" />
                </Border>
                <TextBlock Margin="10" Text="{lex:Loc System_Installation_FreeSpaceCalculating}" />
            </StackPanel>
            <TextBlock Grid.Row="0" Margin="10" Visibility="{Binding IsFreeSpaceReady, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}">
                <TextBlock.Inlines>
                    <Run Text="{Binding FreeSpace}" />
                    <Run Foreground="{DynamicResource ThemeSecondaryTextColor}" Text="{lex:Loc System_Installation_SpaceFree}" />
                </TextBlock.Inlines>
            </TextBlock>
        </Grid>

        <!--  Items View  -->
        <Grid Grid.Row="1">
            <TextBlock x:Name="NoApplicationsView" FontSize="{DynamicResource H1}" Text="{lex:Loc System_Installation_NoApplications}" />

            <UserControl x:Name="LoadingView" Height="180" Panel.ZIndex="10" Template="{DynamicResource AppleSpinner}" />

            <Border BorderBrush="{DynamicResource ThemeBorderColor}" BorderThickness="1" CornerRadius="5">
                <ListView
                    x:Name="Items"
                    HorizontalContentAlignment="Stretch"
                    ItemContainerStyle="{DynamicResource ListViewItemForUniversalItem}"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    Style="{StaticResource ListViewStyleWithoutColumns}"
                    VirtualizingPanel.ScrollUnit="Pixel" />
            </Border>
        </Grid>


        <TextBlock
            x:Name="SelectedItemsCount"
            Grid.Row="2"
            VerticalAlignment="Center"
            Foreground="Red" Text="Items Selected go here" TextAlignment="Right" Visibility="Collapsed" />

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button
                x:Name="Uninstall"
                Grid.Column="0"
                MaxWidth="360" MaxHeight="60"
                Margin="15"
                Content="{lex:Loc System_Installation_Uninstall}"
                FontSize="{DynamicResource H5}"
                IsEnabled="{Binding CanUninstall}"
                Style="{DynamicResource ButtonBaseStyle}" />
            <Button
                Grid.Column="1"
                Width="120" Height="60"
                Margin="15"
                Content="Clear Selection"
                FontSize="{DynamicResource H5}"
                Style="{DynamicResource ButtonBaseStyle}"
                Visibility="Collapsed" />
        </Grid>

    </Grid>
</UserControl>

