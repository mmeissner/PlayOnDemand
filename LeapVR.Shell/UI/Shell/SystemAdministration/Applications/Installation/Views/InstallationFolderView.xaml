<UserControl
    x:Class="LeapVR.Shell.UI.Shell.SystemAdministration.Applications.Installation.Views.InstallationFolderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="100" d:DesignWidth="600"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style BasedOn="{StaticResource TextBlockBaseStyle}" TargetType="TextBlock">
            <Setter Property="FontSize" Value="{DynamicResource H5}" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <ContentControl ContentTemplate="{DynamicResource InstallationLoadingItem}" Visibility="{Binding IsLoaded, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=true, FallbackValue=Collapsed}" />

        <Grid x:Name="DataPane" Visibility="{Binding IsLoaded, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Visible}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="120" />
            </Grid.ColumnDefinitions>
            <Image
                Grid.Row="0" Grid.Column="0"
                Width="92" Height="43"
                Margin="5"
                Source="{Binding Thumbnail, Mode=OneWay}" />
            <TextBlock
                x:Name="Title"
                Grid.Row="0" Grid.Column="1"
                Margin="5,0,5,0" HorizontalAlignment="Stretch"
                Text="Folder Name" />
        </Grid>
    </Grid>
</UserControl>

