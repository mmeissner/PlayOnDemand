<UserControl
    x:Class="LeapVR.Shell.UI.Shell.SystemAdministration.PowerOff.Views.TabItemPoweroffView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:lex="http://wpflocalizeextension.codeplex.com" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:properties="clr-namespace:LeapVR.Shell.Properties"
    d:DesignHeight="720" d:DesignWidth="1280"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            FontSize="{DynamicResource H1}"
            Style="{DynamicResource TextBlockBaseStyle}"
            Text="{lex:Loc System_Poweroff_DoYouReallyWantToPoweroff}"
            TextAlignment="Center" />
        <Button
            x:Name="Poweroff"
            Grid.Row="1"
            Width="500" Height="80"
            VerticalAlignment="Top"
            BorderThickness="3"
            Content="{lex:Loc System_Poweroff_PoweroffNow}"
            FontSize="{DynamicResource H3}"
            Style="{DynamicResource ButtonBaseStyle}" />
    </Grid>
</UserControl>
