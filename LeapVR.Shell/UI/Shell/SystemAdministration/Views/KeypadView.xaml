<UserControl
    x:Class="LeapVR.Shell.UI.Shell.SystemAdministration.Views.KeypadView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cal="http://www.caliburnproject.org" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions" xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:lex="http://wpflocalizeextension.codeplex.com" xmlns:local="clr-namespace:LeapVR.Shell.UI.Shell.SystemAdministration.Views" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:usercontrols="clr-namespace:LeapVR.Shell.UI.Usercontrols"
    d:DesignHeight="768" d:DesignWidth="1024"
    mc:Ignorable="d">
    <Viewbox MaxHeight="720">
        <Grid x:Name="PanelKeyPad">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock
                x:Name="InstructionMessage"
                Margin="0,30"
                FontSize="{DynamicResource H3}"
                Style="{DynamicResource TextBlockBaseStyle}"
                Text="{lex:Loc System_EnterPin}"
                TextAlignment="Center" />

            <StackPanel
                x:Name="PanelLanguage"
                Grid.Row="1"
                Margin="0,30" HorizontalAlignment="Center"
                Orientation="Horizontal">
                <RadioButton
                    x:Name="Chinese"
                    Width="300" Height="100"
                    Margin="20,0" HorizontalAlignment="Center" VerticalAlignment="Center" HorizontalContentAlignment="Center"
                    VerticalContentAlignment="Center"
                    Content="简体中文" Focusable="False"
                    FontSize="{DynamicResource H2}"
                    GroupName="Language"
                    IsChecked="{Binding CurrentCultureName, Mode=OneWay, Converter={StaticResource CultureMatchToBoolConverter}, ConverterParameter='zh-CN'}"
                    Style="{DynamicResource RadioButtonBaseStyle}" />

                <RadioButton
                    x:Name="English"
                    Width="300" Height="100"
                    Margin="20,0" HorizontalAlignment="Center" VerticalAlignment="Center" HorizontalContentAlignment="Center"
                    VerticalContentAlignment="Center"
                    Content="English" Focusable="False"
                    FontSize="{DynamicResource H2}"
                    GroupName="Language"
                    IsChecked="{Binding CurrentCultureName, Mode=OneWay, Converter={StaticResource CultureMatchToBoolConverter}, ConverterParameter='en-US'}"
                    Style="{DynamicResource RadioButtonBaseStyle}" />
            </StackPanel>

            <usercontrols:KeyPad
                x:Name="KeyPad"
                Grid.Row="2"
                Width="498" Height="600"
                cal:Message.Attach="                  [Event ValueChanged] = [Action OnKeypadValueChanged(KeyPad.Value)];                  [Event InputComplete] = [Action OnKeypadInputComplete(KeyPad.Result)]             "
                DisplayText="{Binding PinDisplay, Mode=OneWay}"
                Focusable="True" />

        </Grid>
    </Viewbox>
</UserControl>
