<UserControl
    x:Class="LeapVR.Shell.UI.Shell.SystemAdministration.Security.Views.TabItemSecurityView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cal="http://www.caliburnproject.org" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:usercontrols="clr-namespace:LeapVR.Shell.UI.Usercontrols"
    d:DesignHeight="720" d:DesignWidth="1280"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style BasedOn="{StaticResource TextBlockBaseStyle}" TargetType="TextBlock">
            <Setter Property="FontSize" Value="{DynamicResource H3}" />
            <Setter Property="Margin" Value="10,0,0,0" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>

    <Viewbox MaxHeight="640">
        <Grid>
            <DockPanel VerticalAlignment="Center">
                <TextBlock x:Name="InstructionMessage" Margin="0,20" DockPanel.Dock="Top" Text="Please enter old pin." />
                <usercontrols:KeyPad
                    x:Name="KeyPad"
                    Width="498" Height="600"
                    VerticalAlignment="Center"
                    cal:Message.Attach="                 [Event ValueChanged] = [Action OnKeyPadValueChanged(KeyPad.Value)];                  [Event InputComplete] = [Action OnKeyPadInputComplete(KeyPad.Result)]              "
                    DisplayText="{Binding PinDisplay, Mode=OneWay}"
                    DockPanel.Dock="Top"
                    IsEnabled="{Binding Locked, Converter={StaticResource InvertBoolConverter}}" />
            </DockPanel>
        </Grid>
    </Viewbox>
</UserControl>
