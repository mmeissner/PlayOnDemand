<UserControl
    x:Class="LeapVR.Shell.UI.Shell.SystemAdministration.Accounts.Views.PlatformAccountAppsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:lex="http://wpflocalizeextension.codeplex.com" xmlns:local="clr-namespace:LeapVR.Shell.UI.Shell.SystemAdministration.Applications.Platform.Views" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="954" d:DesignWidth="1720"
    mc:Ignorable="d">
    <Grid Background="Transparent">

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" MinHeight="100" />
        </Grid.RowDefinitions>
        <ContentControl x:Name="UnlicensedApplications" Grid.Row="0" Grid.Column="1" Margin="25" />
        <ContentControl x:Name="AssignedApplications" Grid.Row="0" Grid.Column="0" Margin="25" />
        <Button
            x:Name="RemoveLicense"
            Grid.Row="1" Grid.Column="0"
            MinWidth="300"
            Margin="15" HorizontalAlignment="Center"
            Content="{lex:Loc System_Account_RemoveLicense}"
            FontSize="{DynamicResource H5}"
            Style="{DynamicResource ButtonBaseStyle}" />
        <Button
            x:Name="AddLicense"
            Grid.Row="1" Grid.Column="1"
            MinWidth="300"
            Margin="15" HorizontalAlignment="Center"
            Content="{lex:Loc System_Account_AddLicense}"
            FontSize="{DynamicResource H5}"
            Style="{DynamicResource ButtonBaseStyle}" />
    </Grid>
</UserControl>