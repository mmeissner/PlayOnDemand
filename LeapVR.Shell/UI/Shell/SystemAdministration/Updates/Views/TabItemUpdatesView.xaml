<UserControl
    x:Class="LeapVR.Shell.UI.Shell.SystemAdministration.Updates.Views.TabItemUpdatesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:lex="http://wpflocalizeextension.codeplex.com" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="720" d:DesignWidth="1280"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="4*" />
        </Grid.RowDefinitions>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
            <TextBlock
                Margin="15,0" VerticalAlignment="Bottom"
                FontSize="{DynamicResource H3}"
                Style="{DynamicResource TextBlockBaseStyle}"
                TextAlignment="Center">
                <TextBlock.Inlines>
                    <Run Foreground="{DynamicResource ThemeDisabledTextColor}" Text="{lex:Loc System_Updates_CurrentVersion}" />
                    <Span>:</Span>
                    <Run Text="{Binding CurrentVersion, Mode=OneWay}" />
                </TextBlock.Inlines>
            </TextBlock>
            <TextBlock
                Margin="15,0" VerticalAlignment="Bottom"
                FontSize="{DynamicResource H3}"
                Style="{DynamicResource TextBlockBaseStyle}"
                TextAlignment="Center"
                Visibility="{Binding IsNewVersionAvailable, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock.Inlines>
                    <Run Foreground="{DynamicResource ThemeDisabledTextColor}" Text="{lex:Loc System_Updates_OnlineVersion}" />
                    <Span>:</Span>
                    <Run Text="{Binding NewestVersion, Mode=OneWay}" />
                </TextBlock.Inlines>
            </TextBlock>

        </StackPanel>

        <ContentControl x:Name="ActiveItem" Grid.Row="1" />

    </Grid>
</UserControl>
