<UserControl
    x:Class="LeapVR.Shell.UI.Shell.SystemAdministration.Statistics.Views.ApplicationStatisticsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cal="http://www.caliburnproject.org" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:lex="http://wpflocalizeextension.codeplex.com" xmlns:local="clr-namespace:LeapVR.Shell.UI.Shell.SystemAdministration.Statistics.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:properties="clr-namespace:LeapVR.Shell.Properties"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style BasedOn="{StaticResource TextBlockBaseStyle}" TargetType="TextBlock">
            <Setter Property="FontSize" Value="{DynamicResource H5}" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>

    <Grid x:Name="Container">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="3*" />

        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="{Binding Number}" />
        <ContentControl
            x:Name="Thumbnail"
            Grid.Column="1"
            Width="92" Height="43"
            cal:View.Context="Small" />
        <TextBlock Grid.Column="2" Text="{Binding Name}" />
        <TextBlock Grid.Column="3" Text="{Binding LastTimePlayed, FallbackValue='-'}" />
        <TextBlock Grid.Column="4" Text="{Binding TimesPlayed, FallbackValue='-'}" />
        <TextBlock Grid.Column="5" Text="{Binding LongestSession, FallbackValue='-'}" />
        <TextBlock Grid.Column="6" Text="{Binding TotalRuntime, FallbackValue='-'}" />

    </Grid>
</UserControl>
