<UserControl
    x:Class="LeapVR.Shell.UI.Shell.Blocker.Views.BusyShellBlockingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="600" d:DesignWidth="1000"
    mc:Ignorable="d">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
            <UserControl Width="{DynamicResource H2}" Foreground="{DynamicResource ThemeMainTextColor}" Template="{DynamicResource AppleSpinner}" />
            <TextBlock
                x:Name="TextDotIncreasing"
                FontSize="{DynamicResource H2}"
                Style="{DynamicResource TextBlockBaseStyle}"
                Text="Loading..." TextAlignment="Center" />

        </StackPanel>
    </Grid>
</UserControl>
