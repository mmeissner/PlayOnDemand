<Window
    x:Class="Unosquare.FFME.Windows.Sample.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Unosquare.FFME.Windows.Sample.Controls" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:ffme="clr-namespace:Unosquare.FFME;assembly=ffme.win" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="window"
    Title="{Binding WindowTitle}"
    Width="0" Height="0" MinWidth="1280" MinHeight="720"
    Background="Black"
    DataContext="{StaticResource ViewModel}"
    WindowStartupLocation="Manual"
    mc:Ignorable="d">
    <Window.Resources>
        <Storyboard x:Key="ShowControlOpacity" Storyboard.TargetProperty="(UIElement.Opacity)">
            <DoubleAnimation FillBehavior="HoldEnd" To="1" Duration="0:0:0.100" />
        </Storyboard>
        <Storyboard x:Key="HideControlOpacity" Storyboard.TargetProperty="(UIElement.Opacity)">
            <DoubleAnimation FillBehavior="HoldEnd" To="0" Duration="0:0:0.300" />
        </Storyboard>
    </Window.Resources>
    <Grid Name="Player">
        <ffme:MediaElement Name="Media" Background="Black" LoadedBehavior="Play" />

        <DockPanel x:Name="LayoutPanel" LastChildFill="False">

            <controls:ControllerPanelControl x:Name="ControllerPanel" DockPanel.Dock="Bottom" Visibility="{Binding IsApplicationLoaded, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <controls:PlaylistPanelControl x:Name="PlaylistPanel" DockPanel.Dock="Right" Visibility="{Binding IsPlaylistPanelOpen, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <controls:PropertiesPanelControl x:Name="PropertiesPanel" DockPanel.Dock="Right" Visibility="{Binding IsPropertiesPanelOpen, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </DockPanel>

    </Grid>

</Window>
